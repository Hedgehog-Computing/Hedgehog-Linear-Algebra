function trace(inputMatrix:Mat) {
    traceVal=0;

    for(i=0;i<inputMatrix.rows;i++) {
        for (j=0; j<inputMatrix.cols;j++) {
            if (i == j) {
                traceVal += inputMatrix.val[i][j]
            }
        }
    }
    return traceVal;
}
